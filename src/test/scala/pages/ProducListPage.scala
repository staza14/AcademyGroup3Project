package pages

import locators.InventoryPageLoc.gridLayout
import org.openqa.selenium.{By, WebDriver}

object ProducListPage extends BasePage {


/*

    def isProductGridVisible(driver: WebDriver): Boolean = {
      try {
        val element = driver.findElement(gridLayout)
        element.isDisplayed
      } catch {
        case _: Throwable => false
      }

 */



}